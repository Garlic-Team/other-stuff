import type { GClient } from '../GClient';
export declare class Plugin {
    name: string;
    run: (client: GClient) => any;
    constructor(name: string, run: (client: GClient) => any);
}
//# sourceMappingURL=Plugin.d.ts.map